lzc-sdk-version: 0.1
usage: |
  早起的鸟儿有虫吃。
  Early bird Gets bugs.
  Lazy Bird GetsNo Bug!
application:
  multi_instance: true
  subdomain: lazymanga
  routes:
    - /=file:///lzcapp/pkg/content/web
    - >-
      /api/=exec://3000,/lzcapp/pkg/content/lazymanga --db
      /lzcapp/var/lazymanga.db
ext_config:
  enable_media_access: true
